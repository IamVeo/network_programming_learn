<?xml version="1.0" encoding="UTF-8"?>

<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="
             http://maven.apache.org/POM/4.0.0
             https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>org.example</groupId>
    <artifactId>SOAPClient</artifactId>
    <version>1.0-SNAPSHOT</version>

    <!-- ================================ -->
    <!-- Java version                     -->
    <!-- ================================ -->
    <properties>
        <maven.compiler.release>25</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>


    <!-- ================================ -->
    <!-- Dependencies                     -->
    <!-- ================================ -->
    <dependencies>

        <!-- Jakarta XML Web Services API -->
        <dependency>
            <groupId>jakarta.xml.ws</groupId>
            <artifactId>jakarta.xml.ws-api</artifactId>
            <version>4.0.2</version>
        </dependency>


        <!-- JAX-WS / Metro runtime -->
        <dependency>
            <groupId>com.sun.xml.ws</groupId>
            <artifactId>jaxws-rt</artifactId>
            <version>4.0.5</version>
        </dependency>


        <!-- JAXB API -->
        <dependency>
            <groupId>jakarta.xml.bind</groupId>
            <artifactId>jakarta.xml.bind-api</artifactId>
            <version>4.0.2</version>
        </dependency>


        <!-- JAXB runtime -->
        <dependency>
            <groupId>org.glassfish.jaxb</groupId>
            <artifactId>jaxb-runtime</artifactId>
            <version>4.0.5</version>
        </dependency>

        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <version>9.4.0</version>
        </dependency>

    </dependencies>


    <!-- ================================ -->
    <!-- Build                            -->
    <!-- ================================ -->
    <build>

        <plugins>

            <!-- Compile bằng JDK 25 -->
            <plugin>
                <groupId>com.sun.xml.ws</groupId>
                <artifactId>jaxws-maven-plugin</artifactId>
                <version>4.0.5</version>

                <executions>
                    <execution>
                        <id>generate-data-service</id>
                        <phase>generate-sources</phase>

                        <goals>
                            <goal>wsimport</goal>
                        </goals>

                        <configuration>

                            <wsdlUrls>
                                <wsdlUrl>
                                    http://36.50.135.242:2221/DataService?wsdl
                                </wsdlUrl>
                            </wsdlUrls>

                            <packageName>
                                org.example.ws.data
                            </packageName>

                            <keep>true</keep>
                            <verbose>true</verbose>
                            <xnocompile>true</xnocompile>
                            <extension>true</extension>

                        </configuration>
                    </execution>


                    <!-- CharacterService -->
                    <execution>
                        <id>generate-character-service</id>
                        <phase>generate-sources</phase>

                        <goals>
                            <goal>wsimport</goal>
                        </goals>

                        <configuration>

                            <wsdlUrls>
                                <wsdlUrl>
                                    http://36.50.135.242:2221/CharacterService?wsdl
                                </wsdlUrl>
                            </wsdlUrls>

                            <packageName>
                                org.example.ws.character
                            </packageName>

                            <keep>true</keep>
                            <verbose>true</verbose>
                            <xnocompile>true</xnocompile>
                            <extension>true</extension>

                        </configuration>
                    </execution>

                    <execution>
                        <id>generate-object-service</id>
                        <phase>generate-sources</phase>

                        <goals>
                            <goal>wsimport</goal>
                        </goals>

                        <configuration>

                            <wsdlUrls>
                                <wsdlUrl>
                                    http://36.50.135.242:2221/ObjectService?wsdl
                                </wsdlUrl>
                            </wsdlUrls>

                            <packageName>
                                org.example.ws.object
                            </packageName>

                            <keep>true</keep>
                            <verbose>true</verbose>
                            <xnocompile>true</xnocompile>
                            <extension>true</extension>

                        </configuration>
                    </execution>
                </executions>
            </plugin>

        </plugins>

    </build>

</project>